export function LoginPage() {
  return (
    <div className="flex min-h-screen items-center justify-center bg-[hsl(var(--background))]">
      <div className="w-full max-w-sm space-y-6 rounded-lg border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-8 shadow-lg">
        <h1 className="text-2xl font-brand text-center text-[hsl(var(--secondary))]">VPC Music</h1>
        {/* TODO: login form */}
        <p className="text-center text-[hsl(var(--muted-foreground))]">Login coming soon.</p>
      </div>
    </div>
  );
}
