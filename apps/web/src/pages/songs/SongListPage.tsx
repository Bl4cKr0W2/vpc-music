export function SongListPage() {
  return (
    <div className="space-y-4">
      <h2 className="text-2xl font-brand text-[hsl(var(--foreground))]">Songs</h2>
      {/* TODO: song list with search, filter, import */}
      <p className="text-[hsl(var(--muted-foreground))]">No songs yet. Import your library to get started.</p>
    </div>
  );
}
