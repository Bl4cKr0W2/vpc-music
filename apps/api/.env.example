# ── Database ──────────────────────────────────────
# No local Postgres — dev uses the staging CloudClusters DB.
DATABASE_URL=postgresql://admin:jvpfgZ-W_s9*duvxN*EW@postgresql-204635-0.cloudclusters.net:10061/vpc-music-stg

# ── API ───────────────────────────────────────────
PORT=3001
NODE_ENV=development
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=7d
CORS_ORIGIN=http://localhost:5176
FRONTEND_URL=http://localhost:5176

# ── Email (Mailgun SMTP) ─────────────────────────
# MAILGUN_SMTP_HOST=smtp.mailgun.org
# MAILGUN_SMTP_PORT=587
# MAILGUN_SMTP_USER=postmaster@mg.yourdomain.com
# MAILGUN_SMTP_PASS=your-mailgun-smtp-password
# EMAIL_FROM=VPC Music <noreply@yourdomain.com>

# ── PDF Parsing ──────────────────────────────────
# PDF_CO_API_KEY=

# ── OAuth2 — Google ───────────────────────────────
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=http://localhost:5176/api/auth/google/callback
